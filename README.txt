Instrction to Run the project
1) Before running the app please check if all the required libraries are installed and clear the browser cache.
2) If any librariries are not installed, they can easily be installed by "pip install <library_name>"
3) The project can be run either by setting the "FLASK_APP=app" and the flask run from command line or directly executing "python app.py"

https://vancouver-crime-prediction.herokuapp.com/ click on the link to see the project
